define(['psc-tests-assert','Psc/UI/ComboDropBox'], function(t) {
  
  module("Psc.UI.ComboDropBox");
  
  test("TODO: when combobox triggers combo-box-selected item is added to dropbox", function () {
    t.setup(this);

    this.assertTrue(true, 'Test is incomplete');
  });
});